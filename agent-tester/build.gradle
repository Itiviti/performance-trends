dependencies {
    /* NOt necessary in real life, but it allows building the projects in the right order: the agent jar must exist before using it */
    runtime project(':profiler-agent')
}

test {
    jvmArgs "-javaagent:../profiler-agent/build/libs/profiler-agent-1.0-SNAPSHOT.jar"
}